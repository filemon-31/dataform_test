config {
    type: operations
    schema: dl_nothwind
    hasOutput: true
}

CREATE OR REPLACE TABLE
  ${self()}
WITH CONNECTION `${dataform.projectConfig.vars.bigLakeConnection} OPTIONS ( format = "CSV",
    uris = ['gs://dl_northwind_dataset/products.csv'] );
